import React from 'react'

const Roadmap = ({ milestones }) => {
  return (
    <section id="project-roadmap">
      {/* <p className="text-base 4k:text-2xl text-yellow uppercase">About</p>
      <h1 className="mt-1 text-3xl sm:text-4xl lg:text-5xl 4k:text-7xl text-brown-light uppercase">
        Project Roadmap
      </h1>
      <ul className="mt-6 list-disc pl-5 text-brown-light">
        {milestones.map((row, i) => (
          <li key={i} className="py-1">
            {row.milestone}
          </li>
        ))}
      </ul> */}
    </section>
  )
}

export default Roadmap
